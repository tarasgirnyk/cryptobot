"""CryptoBOT — міжбіржовий арбітражний сканер і виконавець.

Пакет розбитий на модулі:
- ``config``     — усі env-параметри та пороги;
- ``util``       — дрібні helpers (HTTP, парсинг чисел);
- ``runtime``    — спільний змінний стан (automation_state, cooldown, paper-позиції);
- ``storage``    — SQLite: audit, персистенція, app_state;
- ``scanner``    — публічні ринкові дані бірж і побудова зв'язок;
- ``funding``    — модель інтервалу фандінгу та часу до нарахування;
- ``depth``      — аналіз стакана (VWAP, прослизання);
- ``paper``      — віртуальні позиції та метрики готовності;
- ``risk``       — risk-фільтр кандидатів;
- ``reports``    — текстові зведення для Telegram;
- ``telegram``   — бот-пульт (команди, кнопки, polling);
- ``automation`` — фоновий цикл сканера/входів/виходів;
- ``app``        — HTTP-сервер і точка входу ``main()``.
"""
